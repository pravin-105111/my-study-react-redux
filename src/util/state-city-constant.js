export const statesLists = [
   {label: "Andhra Pradesh".toUpperCase(), value: "Andhra Pradesh".toUpperCase()},
   {label: "Arunachal Pradesh", value: "Arunachal Pradesh".toUpperCase()}
      // "Assam".toUpperCase(),
   // "BIHAR",
   // "Chandigarh".toUpperCase(),
   // "Chhattisgarh".toUpperCase(),
   // "Dadra and Nagar Haveli".toUpperCase(),
   // "Daman and Diu".toUpperCase(),
   // "Delhi".toUpperCase(),
   // "GOA",
   // "Gujarat".toUpperCase(),
   // "Haryana".toUpperCase(),
   // "Himachal Pradesh".toUpperCase(),
   // "Jammu and Kashmir".toUpperCase(),
   // "Jharkhand".toUpperCase(),
   // "Karnataka".toUpperCase(),
   // "Kerala".toUpperCase(),
   // "Lakshadweep".toUpperCase(),
   // "Madhya Pradesh".toUpperCase(),
   // "Maharashtra".toUpperCase(),
   // "Manipur".toUpperCase(),
   // "Meghalaya".toUpperCase(),
   // "Mizoram".toUpperCase(),
   // "Nagaland".toUpperCase(),
   // "Odisha".toUpperCase(),
   // "Puducherry".toUpperCase(),
   // "Punjab".toUpperCase(),
   // "Rajasthan".toUpperCase(),
   // "Sikkim".toUpperCase(),
   // "Tamil Nadu".toUpperCase(),
   // "Telangana".toUpperCase(),
   // "Tripura".toUpperCase(),
   // "Uttarakhand".toUpperCase(),
   // "Uttar Pradesh".toUpperCase(),
   // "West Bengal".toUpperCase(),
   ]
